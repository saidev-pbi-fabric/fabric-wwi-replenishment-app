# Threads in this folder

Scan this to decode the /resume list. The picker shows each session's first message; match it to a
row here. One row = one work thread (strand). Newest at the top.

| Tag | What it is | Opened | Status | Resume from |
|-----|------------|--------|--------|-------------|
| #wwi-hackathon-build | Continue the Fabric WWI Replenishment App build — push to GitHub, wireframe pass, /spec, /plan | 2026-08-16 | paused | Restart session, then run /spec (see CLAUDE.md status block) |
| #wwi-hackathon-build | Fabric access confirmed, WWI Copy Job run into `WWIWarehouse`, az login + rayfin login done, `fabric-mcp-server` needs restart to re-auth as the right account | 2026-08-17 | resolved | (was: restart session + re-auth + re-explore stale schema — all done same day, see next row) |
| #wwi-hackathon-build | Auth fixed (stale WAM account, not a restart issue), schema doc corrected from real live tables, Phase 2 complete: SM core built+deployed+refreshed, risk DAX measures (T2.1), both pages' query factories (T2.2/T2.3) — all tested, committed, pushed through `225b516` | 2026-08-17 | paused | Continue per `CLAUDE.md` Status (2026-08-17) — next up is T3.x, Page 1 UI (KPI strip, trend chart, ranked list), see `docs/wireframe-design-brief.md` + `docs/design-and-dax-references.md` for the locked design direction |
